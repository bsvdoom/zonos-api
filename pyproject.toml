[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "zonos_api"
version = "0.1.0"
dependencies = [
    "flash-attn",
    "mamba-ssm",
    "causal-conv1d"
]

[tool.setuptools]
packages = { find = { where = ["app", "Zonos"] } }
